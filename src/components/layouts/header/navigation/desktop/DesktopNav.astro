---
import DesktopNavItem from "./DesktopNavItem.astro";
import { NAVIGATION } from "@/constants/navigation.constant";
---

<nav class="gap-6 md:flex">
  {
    NAVIGATION.map((item) => (
      <DesktopNavItem href={item.path} label={item.name} />
    ))
  }
</nav>
